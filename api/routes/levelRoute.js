const { Router } = require('express');
const levelService = require('../controllers/LevelController');

const router = Router();

// router.get('/levels', levelService.index);

module.exports = router;